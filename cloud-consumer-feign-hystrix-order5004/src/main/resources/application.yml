server:
  port: 5004

spring:
  application:
    name: feign-hystrix-order5004
eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/
feign:
  hystrix:
    enabled: true
